<UserControl x:Class="SuggesterApp.TrialModeCtl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Trial Mode" FontSize="40" Grid.Row="0" />
        <StackPanel Grid.Row="1">
            <TextBlock             
                TextWrapping="Wrap" >This trial mode comes with two lists, adjectives &amp; objects.                    
            </TextBlock>
            <TextBlock TextWrapping="Wrap">The full version will give you access locations, emotions, 
                relationships &amp; any lists that I may add later.
            </TextBlock>
        </StackPanel>
        <Button Grid.Row="2" Name="_btnBuyNow" Click="_btnBuyNow_Click" >Buy now</Button>
    </Grid>
</UserControl>
